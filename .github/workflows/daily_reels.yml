name: Daily Instagram Reels Autopilot (DEPRECATED)

# DEPRECATED: daily-video.yml is the single authoritative 3x-daily publisher
# (10:30 / 14:30 / 20:30 IST). This workflow is parked with NO schedule and NO
# repository_dispatch triggers so duplicate publishing is impossible.
# Kept for manual inspection only.

on:
  workflow_dispatch:
    inputs:
      topic:
        description: "Deprecated — use daily-video.yml instead"
        required: false
        default: ""

concurrency:
  group: deprecated-daily-reels
  cancel-in-progress: false

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "DEPRECATED: use .github/workflows/daily-video.yml (authoritative 60s pipeline)."
          echo "This workflow performs no rendering or publishing."
